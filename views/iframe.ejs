<html>
  <head>
    <link rel="stylesheet" href="/main.css" />
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@600&display=swap" rel="stylesheet">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/js/bootstrap.bundle.min.js" integrity="sha384-JEW9xMcG8R+pH31jmWH6WWP0WintQrMb4s7ZOdauHnUtxwoG2vI5DkLtS3qm9Ekf" crossorigin="anonymous"></script>
  </head>
  <body>
    <br />
    <h2 class="txt">Use this secure generated password</h2>
    <code><h1 class="txt" id="pw"></h1></code>
    <input id="hiddenInput" style="display:none;" type="text"value='' />
    <button class="btn" id="regen">Regenerate</button>
    <button id="copybtn" class="btn">Copy</button>
    <a href="/why" target="_blank"><button class="btn">Why?</button></a>
    <br /><br />
    <disclaimer>Powered by <a href="https://spg.ar-dev.cf" target="_blank">SPG</a></disclaimer>
    </center>
    <script>
        function gen(length = 30) {
        var buf = new Uint8Array(length);window.crypto.getRandomValues(buf);
        var value = btoa(String.fromCharCode.apply(null, buf)).replace(/\//g,"_").replace(/\+/g,"").replace(/\=/g,"").substring(0,length)
        document.getElementById('pw').innerHTML = value
        document.getElementById('hiddenInput').value = value
        }
        gen()
    </script>
    <script>
    const copy = document.getElementById("copybtn");
    const code = document.getElementById("hiddenInput");
        copy.onclick = function() {
        code.setAttribute("style", "display: initial;");
        code.select();
        document.execCommand("copy");
        code.setAttribute("style", "display: none;");
        copy.innerText = "Copied!";
        setTimeout(function() {
          copy.innerText = 'Copy';
        }, 750);
        };
    </script>
    <script>
    const refresh = document.getElementById('regen')
    refresh.onclick = function() {
    function gen(length = 30) {
        var buf = new Uint8Array(length);window.crypto.getRandomValues(buf);
        var value = btoa(String.fromCharCode.apply(null, buf)).replace(/\//g,"_").replace(/\+/g,"").replace(/\=/g,"").substring(0,length)
        document.getElementById('pw').innerHTML = value
        document.getElementById('hiddenInput').value = value
        }
        gen()
    }
    </script>
  </body>
</html>